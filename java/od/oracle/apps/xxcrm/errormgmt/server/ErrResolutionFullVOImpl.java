package od.oracle.apps.xxcrm.errormgmt.server;
import oracle.apps.fnd.framework.server.OAViewObjectImpl;

import oracle.jbo.domain.Number;
import oracle.apps.fnd.framework.OAException;
//  ---------------------------------------------------------------
//  ---    File generated by Oracle Business Components for Java.
//  ---------------------------------------------------------------

public class ErrResolutionFullVOImpl extends OAViewObjectImpl 
{
  /**
   * 
   * This is the default constructor (do not remove)
   */
  public ErrResolutionFullVOImpl()
  {
  }
  public void initQuery(String resolutionId)
  {
     if ((resolutionId != null) && (!("".equals(resolutionId.trim()))))
     {
       Number resId = null;
       try
       {
         resId = new Number(resolutionId);
       }
       catch(Exception e)
       {
         throw new OAException("AK", "FWK_TBX_INVALID_EMP_NUMBER");
       }
       
       setWhereClause("RESOLUTION_ID = :1");
       setWhereClauseParams(null); // Always reset
       setWhereClauseParam(0, resId);
       executeQuery();

     }
  }
}