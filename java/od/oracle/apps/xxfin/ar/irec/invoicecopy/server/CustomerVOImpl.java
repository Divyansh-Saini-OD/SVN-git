// +======================================================================================+
// | Office Depot - Project Simplify                                                      |
// | Providge Consulting                                                                  |
// +======================================================================================+
// |  Class:         CustomerVOImpl.java                                                  |
// |  Description:   This class is view object implementation class for the CustomerVO    |
// |                                                                                      |
// |  Change Record:                                                                      |
// |  ==========================                                                          |
// |Version   Date          Author             Remarks                                    |
// |=======   ===========   ================   ========================================== |
// |1.0       26-JUN-2007   BLooman            Initial version                            |
// |                                                                                      |
// +======================================================================================+
package od.oracle.apps.xxfin.ar.irec.invoicecopy.server; 

import oracle.apps.fnd.common.VersionInfo;
import oracle.apps.fnd.framework.server.OAViewObjectImpl;
import oracle.apps.fnd.framework.OAException;
import oracle.jbo.domain.Number;
//  ---------------------------------------------------------------
//  ---    File generated by Oracle Business Components for Java.
//  ---------------------------------------------------------------

public class CustomerVOImpl extends OAViewObjectImpl 
{
  public static final String RCS_ID="$Header$ CustomerVOImpl.java 115.10 2007/07/18 03:00:00 bjl noship ";
  public static final boolean RCS_ID_RECORDED =
        VersionInfo.recordClassVersion(RCS_ID, "od.oracle.apps.xxfin.ar.irec.invoicecopy.server");

  /**
   * 
   * This is the default constructor (do not remove)
   */
  public CustomerVOImpl()
  {
  }

  // initQuery is used to set the where condition on the CustomerVO
  //   object to the given Customer Account Id parameter and execute the query
  public void initQuery(String custAccountId) {
    // check that parameter is defined and convert to number
    Number custAccountIdCnv;
    if ((custAccountId != null) && (!("".equals(custAccountId.trim()))) ) {
      custAccountIdCnv = null;
    }
    try {
      custAccountIdCnv = new Number(custAccountId);
    }
    catch(Exception e) {
      throw new OAException("Customer Account Id must be a number");
    }
    // set where clause and execute query if the CustomerVO object exists
    setWhereClause("cust_account_id = :1" );
    setWhereClauseParams(null);      // Always reset
    setWhereClauseParam(0, custAccountIdCnv);
    executeQuery();
  }

  // initQuery is used to set the where condition on the CustomerVO
  //   object to the given Customer Account Number parameter and execute the query
  public void initQueryNumber(String accountNumber) {
    // set where clause and execute query if the CustomerVO object exists
    setWhereClause("account_number = :1");
    setWhereClauseParams(null);      // Always reset
    setWhereClauseParam(0, accountNumber);
    executeQuery();
  }
}