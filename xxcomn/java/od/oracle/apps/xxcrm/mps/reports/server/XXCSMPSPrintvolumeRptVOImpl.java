package od.oracle.apps.xxcrm.mps.reports.server;
import oracle.apps.fnd.framework.server.OAViewObjectImpl;
//  ---------------------------------------------------------------
//  ---    File generated by Oracle Business Components for Java.
//  ---------------------------------------------------------------

public class XXCSMPSPrintvolumeRptVOImpl extends OAViewObjectImpl {
  /**
   * 
   * This is the default constructor (do not remove)
   */
  public XXCSMPSPrintvolumeRptVOImpl()
  {
  }
  public void initMPSPrintVolumeRpt(String customerName, String ProgramType ,String StartDate, String EndDate, String managedStatus, String activeStatus)
    {
        System.out.println("##### in AM initMPSTOPCustRptVO ProgramType="+ProgramType);
        System.out.println("##### in AM initMPSTOPCustRptVO StartDate="+StartDate);
        StringBuffer whereClause = new StringBuffer();
        setWhereClause(null);
        setWhereClauseParams(null);  
        setWhereClauseParam(0,customerName);
        setWhereClauseParam(1,ProgramType);
        setWhereClauseParam(2,StartDate);
        setWhereClauseParam(3,EndDate);  
        if(managedStatus != null && !"".equals(managedStatus))
        {
            if(whereClause.length() != 0)
                whereClause = whereClause.append(" AND ");
            whereClause = whereClause.append("MANAGED_STATUS ='" + managedStatus + "'");
        }
        if(activeStatus != null && !"".equals(activeStatus))
        {
           if(whereClause.length() != 0)
               whereClause = whereClause.append(" AND ");
           whereClause = whereClause.append("ACTIVE_STATUS ='" + activeStatus + "'");
        }
        if(whereClause.length() != 0)
        {
         setWhereClause(whereClause.toString());          
        } 
        System.out.println("##### Query=" + getQuery());
        executeQuery();
        System.out.println("In VO rowcount=" + getRowCount());
    }
}