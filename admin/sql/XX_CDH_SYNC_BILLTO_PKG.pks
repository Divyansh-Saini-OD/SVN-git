create or replace
PACKAGE XX_CDH_SYNC_BILLTO_PKG AS 

  /* TODO enter package declarations (types, exceptions, methods etc) here */ 

PROCEDURE main (p_batch_id IN NUMBER
                );

END XX_CDH_SYNC_BILLTO_PKG;
/