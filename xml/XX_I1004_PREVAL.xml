<?xml version="1.0" encoding="WINDOWS-1252" ?>
<dataTemplate name="XX_I1004_PREVAL" description="OD: APC_Interface Prevalidation" Version="1.0">
<parameters/>
<dataQuery>
<sqlStatement name="Q_I1004_LOOKUPS">
<![CDATA[
SELECT 'Inventory' as Module,'MTL_FUNCTIONAL_AREAS' AS LOOKUP_NAME,CASE COUNT(1) 
                                           WHEN 1 THEN 'Y'
                                           ELSE 'N'
                                           END AS DOES_EXIST
FROM  mfg_lookups MFL
WHERE MFL.lookup_type = 'MTL_FUNCTIONAL_AREAS'
AND   MFL.meaning = 'Inventory'
AND   MFL.enabled_flag = 'Y'
UNION
SELECT 'Product Reporting' as Module,'MTL_FUNCTIONAL_AREAS' AS LOOKUP_NAME,CASE COUNT(1) 
                                           WHEN 1 THEN 'Y'
                                           ELSE 'N'
                                           END AS DOES_EXIST
FROM  mfg_lookups MFL
WHERE MFL.lookup_type = 'MTL_FUNCTIONAL_AREAS'
AND   MFL.meaning = 'Product Reporting'
AND   MFL.enabled_flag = 'Y'
]]>
</sqlStatement>
<sqlStatement name="Q_I1004_DBPACKAGES">
<![CDATA[
SELECT 'Package Specification' AS Type,'XX_APC_ITEM_CATEGORY_PKG.pks' as Package_Name,CASE COUNT(1) 
                                           WHEN 1 THEN 'Y'
                                           ELSE 'N'
                                           END AS DOES_EXIST
FROM  dba_objects DBO
WHERE DBO.object_name = 'XX_APC_ITEM_CATEGORY_PKG'
AND   DBO.object_type = 'PACKAGE'
AND   DBO.status      = 'VALID'
AND   DBO.owner       = 'APPS'
UNION
SELECT 'Package Body' AS Type,'XX_APC_ITEM_CATEGORY_PKG.pkb' as Package_Name,CASE COUNT(1) 
                                           WHEN 1 THEN 'Y'
                                           ELSE 'N'
                                           END AS DOES_EXIST
FROM  dba_objects DBO
WHERE DBO.object_name = 'XX_APC_ITEM_CATEGORY_PKG'
AND   DBO.object_type = 'PACKAGE BODY'
AND   DBO.status      = 'VALID'
AND   DBO.owner       = 'APPS'
]]>
</sqlStatement>
<sqlStatement name="Q_I1004_CONC_PROG">
<![CDATA[
SELECT 'OD: APC Synchronize Product Hierarchies' AS CONCURRENT_PROGRAM,CASE COUNT(1) 
                                           WHEN 1 THEN 'Y'
                                           ELSE 'N'
                                           END AS DOES_EXIST
FROM  fnd_concurrent_programs FCP
WHERE FCP.concurrent_program_name = 'XXAPCPRODCAT'
AND   FCP.enabled_flag = 'Y'
UNION
SELECT 'Load Catalog Hierarchy' AS CONCURRENT_PROGRAM,CASE COUNT(1) 
                                           WHEN 1 THEN 'Y'
                                           ELSE 'N'
                                           END AS DOES_EXIST
FROM  fnd_concurrent_programs FCP
WHERE FCP.concurrent_program_name = 'ENI_DEN_INIT'
AND   FCP.enabled_flag = 'Y'
]]>
</sqlStatement>
</dataQuery>
<dataStructure>
<group name="G_I1004_LOOKUPS" source="Q_I1004_LOOKUPS">
<element name="MODULE" value="MODULE" />
<element name="LOOKUP_NAME" value="LOOKUP_NAME" />
<element name="DOES_EXIST" value="DOES_EXIST" />
</group>
<group name="G_I1004_DBPACKAGES" source="Q_I1004_DBPACKAGES">
<element name="TYPE" value="TYPE" />
<element name="PACKAGE_NAME" value="PACKAGE_NAME" />
<element name="DOES_EXIST" value="DOES_EXIST" />
</group>
<group name="G_I1004_CONC_PROG" source="Q_I1004_CONC_PROG">
<element name="CONCURRENT_PROGRAM" value="CONCURRENT_PROGRAM" />
<element name="DOES_EXIST" value="DOES_EXIST" />
</group>
</dataStructure>
</dataTemplate>

